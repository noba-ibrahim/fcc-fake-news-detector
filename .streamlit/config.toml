[server]
headless = true
port = 8501

[browser]
gatherUsageStats = false
```

---

## 📝 CRÉER .python-version

**À LA RACINE du projet, créez un fichier `.python-version` :**

1. **Clic droit sur la racine** `fcc-fake-news-api/`
2. **New File**
3. **Nommez-le :** `.python-version`
4. **Contenu (une seule ligne) :**
```
3.11
```

---

## 📝 MODIFIER requirements.txt

**Remplacez le contenu de `requirements.txt` par :**
```
streamlit>=1.31.0
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.0.3